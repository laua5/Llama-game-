# Game display v1 - importing pygame and creating screen

import pygame
import time
pygame.init()

screen = pygame.display.set_mode((800, 300))  # Screen dimensions (W * H)

time.sleep(5)

pygame.quit()
quit()
